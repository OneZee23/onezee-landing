# Copy to .env on the server and fill in. Never commit the real token.
#
# Cloudflare Zero Trust -> Networks -> Tunnels -> (your tunnel) -> copy the
# connector token (the long string after `--token` in the install command).
TUNNEL_TOKEN=
